## 1. Écrire une fonction somme telle que somme(n) renvoie la somme des entiers de 1 à n.
def somme(n):
    s=0
    for i in range (n+1):
       s+=i
    return s
somme(6)

## 2. Écrire une fonction puissance2 telle que puissance2(n) renvoie la liste
## contenant les puissances de 2 jusqu'à 2**n.
def puissance2(n):
    L=[]
    for i in range (n):
        L.append(2**i)
    return L
puissance2(4)

# 3. Écrire une fonction produit telle que, si L est une liste de nombre,
#produit(L) renvoie la multiplication de tous les éléments de L
def produit(L):
    p=1
    for e in L:
       p*=e
    return p
produit([2, 1, 5, 4])

## 4. Écrire une fonction egal telle que egal(L) renvoie True si tous les éléments de L sont égaux, False sinon.
def egal(L):
    for i in range (len(L)):
        if L[i]==L[i-1]:
            return True
    return False
egal([1,1,1])

# 5. Écrire une fonction carre_parfait telle que carre_parfait(n) 
#renvoie True si n peut être écrit comme le carré d'un entier, False sinon.
from math import sqrt
def carre_parfait(n):
        if (n%sqrt(n))==0:
            return True
        else:
            return False
carre_parfait(100)
    
